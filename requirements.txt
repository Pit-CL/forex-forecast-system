# ==========================================
# FOREX FORECAST SYSTEM - DEPENDENCIES
# ==========================================
# Production dependencies for USD/CLP forecasting system
# Python 3.11+ required
# ==========================================

# Core data processing
pandas>=2.2
numpy>=1.26
pyarrow>=16.0

# HTTP clients and web scraping
requests>=2.32
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.2
defusedxml>=0.7.1  # Secure XML parsing (prevents XXE attacks)
yfinance>=0.2.40  # Yahoo Finance API for copper prices
tenacity>=8.3

# Configuration and environment
python-dotenv>=1.0
pydantic>=2.7
pydantic-settings>=2.6
email-validator>=2.1

# Visualization
matplotlib>=3.9
seaborn>=0.13

# Statistical modeling and forecasting
statsmodels>=0.14
pmdarima>=2.0
arch>=6.3
scikit-learn>=1.5
scipy>=1.11

# Machine Learning - XGBoost and optimization
xgboost>=2.0.0
optuna>=3.0.0
shap>=0.42.0

# Deep learning and time series foundation models
torch>=2.0.0
chronos-forecasting>=1.2.0,<2.0.0  # Pin to 1.x to avoid breaking changes (TO BE REMOVED)
psutil>=5.9

# PDF generation
jinja2>=3.1
weasyprint>=62.3
cairocffi>=1.7
pdfplumber>=0.11

# CLI and utilities
typer[all]>=0.12
loguru>=0.7
cachetools>=5.3
tabulate>=0.9
python-slugify>=8.0
markdown>=3.6

# Testing dependencies
pytest>=8.0
pytest-cov>=4.1
pytest-timeout>=2.2

# ==========================================
# SYSTEM DEPENDENCIES (via apt/brew)
# ==========================================
# WeasyPrint requires:
# - libcairo2
# - libpango-1.0-0
# - libpangocairo-1.0-0
# - libgdk-pixbuf2.0-0
# - libffi-dev
# - shared-mime-info
#
# Install on Debian/Ubuntu:
# sudo apt-get install libcairo2 libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0 libffi-dev shared-mime-info
#
# Install on macOS:
# brew install cairo pango gdk-pixbuf libffi
# ==========================================
portalocker>=2.0.0  # Cross-platform file locking for concurrent access
