<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <style>
        body { font-family: "Helvetica Neue", Arial, sans-serif; margin: 24px; color: #1b1b1b; }
        h1, h2, h3 { color: #0a1f44; }
        table { width: 100%; border-collapse: collapse; margin-bottom: 16px; }
        th, td { border: 1px solid #d0d0d0; padding: 6px 8px; font-size: 12px; }
        th { background-color: #eef2f7; }
        img.chart { width: 100%; margin: 12px 0; border: 1px solid #ddd; }
        .meta { font-size: 12px; color: #555; margin-bottom: 12px; }
    </style>
</head>
<body>
    <div class="meta">
        Generado: {{ generated_at }} | Zona horaria: {{ timezone }}
    </div>
    {% for chart in charts %}
        <img src="{{ chart }}" class="chart" alt="chart">
    {% endfor %}
    <div>{{ body | safe }}</div>
</body>
</html>
