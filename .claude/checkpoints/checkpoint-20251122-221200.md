# Checkpoint 2025-11-22 22:12

## Estado Actual
Se corrigió el indicador "Rate Diff" que mostraba N/A. Ahora muestra 0.87 correctamente.

## Archivos Modificados
```
M api/routers/market_indicators.py  - Fix ruta fred_interest_rates.csv para modo local
```

## Branch
master

## Commit Anterior
eb06682 - fix: conectar gráficos del dashboard a datos históricos reales

## Problema Resuelto Esta Sesión
El indicador Rate Diff (diferencial de tasas CL-US) mostraba N/A porque la ruta `/app/data/raw/fred_interest_rates.csv` es de Docker y no existe en modo local.

## Fix Aplicado
En `api/routers/market_indicators.py:85`:
```python
fred_path = Path("/app/data/raw/fred_interest_rates.csv")
if not fred_path.exists():
    fred_path = Path(__file__).parent.parent.parent / "data" / "raw" / "fred_interest_rates.csv"
```

## Servicios Corriendo
- Backend API: http://localhost:8000
- Dashboard: http://localhost:3000
- rate_differential devuelve: 0.87

## Próximos Pasos
1. Verificar visualmente que Rate Diff muestra 0.87 en el dashboard
2. Hacer commit del cambio
3. Considerar si hay otros paths hardcodeados con /app/

## Contexto para Recovery
```bash
# Verificar rate_differential
curl -s http://localhost:8000/api/market-indicators/live | python3 -c "import sys,json; d=json.load(sys.stdin); print('rate_differential:', d.get('indicators',{}).get('rate_differential',{}).get('value', 'N/A'))"

# Iniciar servicios
cd /Users/rafaelfarias/Documents/Recursos/Proyectos/forex-forecast-system/api
../venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000

cd /Users/rafaelfarias/Documents/Recursos/Proyectos/forex-forecast-system/dashboard
npm run dev
```

## Historial de Sesión
1. Fix gráficos con datos falsos (config.py data_path) - COMMIT eb06682
2. Fix Rate Diff N/A (market_indicators.py) - PENDIENTE COMMIT
