# Checkpoint 2025-11-22 22:05

## Estado Actual
Se corrigió el problema de datos falsos en los gráficos del dashboard USD/CLP. Los gráficos ahora muestran datos históricos reales en lugar de valores generados con Math.random().

## Archivos Modificados
```
M api/services/data_service.py      - Lógica para combinar datos mindicador + yahoo
M api/utils/config.py               - FIX PRINCIPAL: cambió data_path de mock_data a data/
M dashboard/components/detailed-forecast-chart.tsx - Fetch async de datos reales
M dashboard/components/forecast-chart.tsx          - Fetch async de datos reales
```

## Branch
master

## Problema Resuelto
El `data_path` en modo desarrollo apuntaba a `api/mock_data` (carpeta inexistente), causando fallback a datos random. Se cambió a `data/` donde están los CSVs reales.

## Decisiones Tomadas Esta Sesión
1. Usar datos de `data/raw/mindicador_data.csv` + `data/raw/yahoo_finance_data.csv`
2. Crear venv aislado en `/forex-forecast-system/venv/` para no contaminar sistema
3. Priorizar mindicador (más reciente) sobre yahoo (histórico más largo)

## Servicios Corriendo
- Backend API: http://localhost:8000 (uvicorn en venv)
- Dashboard: http://localhost:3000 (Next.js)

## Próximos Pasos
1. Verificar visualmente que los gráficos muestran datos correctos (~$929-931)
2. Considerar hacer commit de los cambios
3. Opcional: actualizar datos de mindicador si se necesitan más recientes

## Contexto para Recovery
```bash
# Iniciar backend
cd /Users/rafaelfarias/Documents/Recursos/Proyectos/forex-forecast-system/api
../venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000

# Iniciar frontend
cd /Users/rafaelfarias/Documents/Recursos/Proyectos/forex-forecast-system/dashboard
npm run dev

# Probar endpoint
curl -s "http://localhost:8000/api/historical?days=5" | python3 -m json.tool
```

## Datos Verificados
- Endpoint `/api/historical?days=5` devuelve valores $929-931 (correcto)
- Antes devolvía ~$970 (datos falsos)
